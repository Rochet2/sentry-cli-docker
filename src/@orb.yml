version: 2.1

description: >
  this orb allows executing sentry commands in a docker container

# orbs:
#   sentry-cli-docker: rochet2/sentry-cli-docker@volatile

# commands:
#   greet:
#     description: "A very simple command for demonstration purposes"
#     steps:
#       - run: echo foo
